import { AppHoverClassDirective } from './app-hover-class.directive'

describe('AppHoverClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHoverClassDirective()
    expect(directive).toBeTruthy()
  })
})
